# DevTools Ignore File
# Supported patterns: *, **, directory/, !negation

# global
.git/
.devtools/
node_modules/
dist/
*.vsix

# Exclude everything by default for testing
# Then allowlist specific files below
*

# tool:codebase_search
# Allow only the AST parser test file for Phase 7 testing
# Must un-ignore the parent directory first (gitignore rule)
!client-workspace/
!client-workspace/ast-parser-test.ts

# tool:codebase_map
