/** Standalone class with no extends or implements. */
export class Standalone {
	value: number = 0;
}

/** Standalone interface — no parents, no subtypes. */
export interface Isolated {
	tag: string;
}
