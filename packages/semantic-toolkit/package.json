{
	"name": "@packages/semantic-toolkit",
	"version": "1.0.0",
	"private": true,
	"description": "AST-native semantic codebase search: parser, chunker, indexer, and retrieval pipeline",
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"confusables": "^1.1.1",
		"ts-morph": "^25.0.0"
	},
	"devDependencies": {
		"vitest": "^4.0.18"
	}
}
